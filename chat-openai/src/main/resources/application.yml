server:
  port: 18080

spring:
  application:
    name: chat-openai
  ai:
    openai:
      api-key: {api-key}
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat
      embedding:
        enabled: false